{
  "main": "lib/index.js",
  "slug": "common/figma",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "release-it": "yarn version --patch && git push --follow-tags"
  },
  "dependencies": {
    "canvas": "^2.6.1",
    "figma-js": "^1.11.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.27"
  }
}
